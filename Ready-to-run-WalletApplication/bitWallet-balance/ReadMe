This module is developed as Springboot application.
This provides balance-check service to the client.
If you want to run under Docker container, please use following commands

entry port 9090:9090

docker build -f Dockerfile -t balance:v0.1 .
docker run -p 9090:9090

If you want to run  as an standalone application not inside without Docker , 
use following  command, this will start application at port 9090.
java -jar target/bitWallet-balance-0.0.1-SNAPSHOT.jar